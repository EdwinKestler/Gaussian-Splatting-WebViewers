# Copy to .env (gitignored). The browser never sees these keys.
# Sidecar: ./semantic_sidecar/launch.sh

XAI_API_KEY=
# VISION_MODEL=grok-4.6
# IMAGINE_MODEL=grok-imagine-image-2.0
# SIDECAR_HOST=127.0.0.1
# SIDECAR_PORT=8766
